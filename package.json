{
  "name": "covalent",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npm run clean -w @covalent/common && npm run clean -w @covalent/core && npm run clean -w @covalent/render",
    "wipe": "npm run clean && rimraf node_modules packages/core/node_modules packages/render/node_modules package-lock.json",
    "init": "npm run wipe && npm install",
    "build": "npm run build -w @covalent/common && npm run build -w @covalent/core && npm run build -w @covalent/render",
    "test:core": "npm run test -w @covalent/core && lcov-result-merger 'packages/**/coverage/lcov.info' > coverage/lcov.info",
    "test:render": "npm run test -w @covalent/render && lcov-result-merger 'packages/**/coverage/lcov.info' > coverage/lcov.info",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "lcov-result-merger": "^5.0.1",
    "rimraf": "6.0.1"
  }
}
